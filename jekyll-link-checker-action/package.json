{
  "name": "jekyll-link-checker-action",
  "version": "1.0.0",
  "description": "A GitHub Action to check for broken links in a Jekyll site",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ncc build index.js --license licenses.txt"
  },
  "keywords": [
    "github",
    "action",
    "jekyll",
    "link",
    "checker"
  ],
  "author": "Bruce Fang",
  "license": "MIT",
  "dependencies": {
    "html-link-checker": "^0.2.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}